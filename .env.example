# 複製這個檔成 .env（.env 已經被 gitignore）
#   Copy-Item .env.example .env

# 只有在你之後要加 LLM 功能時才需要。本專案主線用不到，
# 不要為了用而用 —— 這個專案的價值在推論嚴謹度，不在有沒有接 LLM。
OPENAI_API_KEY=

# Kaggle API 走 ~/.kaggle/kaggle.json，不需要寫在這裡。
