# === 資料處理 ===
pandas>=2.2
numpy>=1.26
pyarrow>=15.0          # parquet 讀寫，把 CSV 壓成 1/5 大小且讀取快 10 倍

# === 建模 ===
scikit-learn>=1.5      # TF-IDF 成分相似度、評估指標
lightgbm>=4.5          # 主力模型：表格資料的首選，比 XGBoost 快
# 註：模型解釋改用 LightGBM 內建的 gain 重要度，未使用 SHAP，
#     因此不列為相依套件。若日後要加做逐筆預測的解釋，再引入 shap。

# === 視覺化 ===
matplotlib>=3.8
seaborn>=0.13
plotly>=5.20           # 互動式漏斗圖 / Sankey 圖

# === 儀表板 ===
streamlit>=1.35

# === 工具 ===
kaggle>=1.6            # 資料下載
tqdm>=4.66             # 進度條（處理 2000 萬列時你會需要它）
