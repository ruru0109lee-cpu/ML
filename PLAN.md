# 專案計畫與求職定位策略

## 0. 先看這一段：定位比專案品質更重要

對抗性分析查到的市場事實（2026 年台灣）：

| 事實 | 數字 | 出處 |
|---|---|---|
| 數位行銷 3-5 年（你現在的層級） | 月薪 45,000–60,000 | 104 + JobSalary, 2026-05 |
| 數位行銷 6-9 年（資深/副理） | 月薪 65,000–80,000 | 同上 |
| 轉職者第一份資料職 | 月薪 35,000–45,000 | 104 薪資情報、Dcard 實例 |
| 台灣「完全遠端」職缺總數（所有職稱、所有年資） | 約 75 個 | Indeed 台灣, 2026-07-31 |
| 要求每週 5 天到辦公室的企業比例 | 30% | 2026 RTO 調查 |

**推論**：

1. **6-8 萬是 6-9 年年資的區間**，不是第一份工作的數字。三個月的專題不可能跳一整個職級。
2. **以「初階數據分析師」應徵 = 減薪**。你現有的 3-5 年行銷＋PM 年資已經值 45-60k；轉職歸零會掉到 4 萬初。
3. **正確走法**：定位成「**會量化增量的行銷企劃**」，讓年資被計入，落在 45-60k 的**頂端**（約 5.5-6.5 萬），而不是重新從底部開始。
4. **遠端與外商在 2026 年是反相關的**。外商正在帶頭 RTO。第一份工作的現實目標是**台北 hybrid**，全遠端當作第 3 年用產出換來的籌碼。
5. 遠端接海外公司＝承攬/EOR，沒有勞保、年終、特休——直接牴觸你寫的「福利好」。

> 這不是要你降低目標，是要你把目標放在會成功的那個順序上：先用年資延續拿到 5.5-6.5 萬 + hybrid，再用 3-5 年爬到 65-80k，遠端當籌碼談。

---

## 1. 履歷關鍵字缺口（最容易補、目前沒補）

台灣資料分析職缺的實際篩選堆疊是：**SQL ＋ BI 工具（Power BI / Tableau / Looker Studio）＋ GA4 ＋ Excel**。

一個只輸出 Gradio + sklearn + Jupyter 的專案，在關鍵字比對上幾乎全部落空。所以本專案**強制包含**：

- [ ] `sql/` 目錄，用 DuckDB 或 SQLite 跑真的分群、世代、漏斗查詢（不是 pandas 假裝）
- [ ] 一張 **Looker Studio**（免費）或 Power BI 儀表板，連到輸出的決策表
- [ ] README 裡明確寫出用到的工具清單

這是整個計畫裡投報率最高的三件事。不要跳過。

---

## 2. 12 週排程

| 週 | 目標 | 可檢查的交付物 |
|---|---|---|
| 1 | 環境 + 資料下載 + schema 盤點 | `python -m src.data --describe` 印出三個資料集的欄位與型別 |
| 2 | 共變數平衡檢定 | 對合成資料與 Hillstrom 各產出一張 SMD 表，能指出哪些欄位是 post-treatment |
| 3 | **評估框架先寫**（不要先寫模型） | Qini / uplift@k 在已知 ground truth 的合成資料上驗證通過 |
| 4 | 兩個誠實的 baseline | 隨機排序、傾向分數（示範為何它是錯的）進結果表 |
| 5 | T-learner | Qini AUC 打敗 baseline，結果表更新 |
| 6 | S-learner + GBDT 版本 | 完整比較表 |
| 7 | 四象限分群 + 反效果客 | 找出 sleeping dogs，寫一段行銷人看得懂的解讀 |
| 8 | 決策輸出 + 利潤模型 | 匯出排序名單 CSV，含 margin / 接觸成本敏感度 |
| 9 | SQL 層 + Looker Studio 儀表板 | 儀表板連得上，可公開連結 |
| 10 | Criteo 規模驗證 | 1,300 萬列跑得完，記錄記憶體與時間 |
| 11 | Gradio + FastAPI 打包 | 上傳 CSV → 回傳決策名單，可展示 |
| 12 | README、結果表、A/B test 設計文件 | `EVALUATION.md`：主要指標、guardrail、MDE、樣本數、天數 |

> **求職要並行，不是做完才開始。** 外商招募週期 6-10 週。第 8 週專案就要能講，第 9-12 週同時投履歷。

### 階段 3（REES46 真實 CRM）只在前面全部做完後才碰
7 個 notebook 代表幾乎沒有參考實作，卡住時沒人可以問。這是差異化的代價。

---

## 3. 技術上必須做對的事

1. **先寫評估、再寫模型。** uplift 沒有 ground truth，模型算錯不會報錯。所以第 3 週用合成資料（有 `ground_truth.json`）先確認 Qini 實作正確。
2. **絕不把 post-treatment 變數當共變數。** 「有沒有開信」是處理後才發生的，放進特徵就毀了因果宣稱。`balance.py` 會標記可疑欄位，但最後要靠你的判斷。
3. **時間切分，不要隨機切分**（處理 REES46 時）。
4. **Qini 分母要用 treated/control 比例校正**，不是直接相減。
5. **固定隨機種子**，`config.py` 裡設一次。
6. **報告信賴區間**，不要只報點估計。bootstrap 就夠。

---

## 4. 要避開的陷阱

- **不要做 Transformer / LoRA。** 你的教材裡有，會很想用。但這個專案的價值在推論嚴謹度，不在模型複雜度。「我找到了會被活動趕跑的客戶」比「我用了 Transformer」有價值得多。
- **不要花超過一個下午美化 Gradio。** 你喜歡美的事物，這是最好玩的部分，也是最容易吃掉兩週的部分。
- **不要做成兩個半成品。** 一個做透的專案勝過功能多但都沒做完。

---

## 5. 目標職缺與公司類型

**職稱**（中英並列，投履歷時兩種都要出現）：
- 行銷數據分析師 / Marketing Data Analyst
- CRM 分析師 / CRM Analyst
- 成長分析師 / Growth Analyst
- 客戶數據平台顧問 / CDP Consultant
- 行銷科技顧問 / MarTech Solutions Consultant

**公司類型**：
- MarTech / CDP 廠商（這類公司最看重「懂行銷又會建模」的混合人才）
- **Appier**（台灣總部、英語工作環境、東京上市，做的就是 AI 行銷與個人化——以你的條件是台灣最合身的一家）
- 跨境電商與電商平台
- 美妝集團的數位/CRM 部門（美妝興趣在這裡直接可讀）
- 電子元件通路商的數位商務團隊（被動元件外貿經歷在這裡是稀有資產）

---

## 6. 履歷橋接句（可直接改寫）

> 我有三到五年電商行銷企劃與專案管理經驗，實際執行過活動投放與 KOL 合作。經濟系的訓練讓我不只看「誰有回應」，而是能區分「誰是因為活動才回應」。這個專案把這個能力做成工具：輸入活動名單，輸出經過因果驗證的投放決策，並量化省下多少預算。我不是轉職進資料領域的新人，我是把行銷決策做得可被驗證的行銷人。

---

## 7. 資安提醒

桌面上有明文的 `apikey.txt`。本專案已設定 `.env` + `.gitignore`。在把任何專案推上 GitHub 之前，先確認：

```powershell
git check-ignore -v .env
```

沒有輸出就代表 `.env` 沒有被忽略，停下來修好再推。
