# 電商轉換漏斗診斷與流失預警系統

> 🚧 **開發中** — 目前完成：資料下載與清洗管線。結論數字尚未產出，所有 `【待填】` 會在對應模組完成後補上。

---

## 這個專案在解決什麼問題

一家美妝電商在 5 個月內累積了 **2000 萬筆**用戶行為，但絕大多數瀏覽沒有變成訂單。營運端知道「轉換率不好」，卻不知道**具體哪個環節在漏、漏掉多少錢、該優先修哪一個**。

這個專案用真實行為資料回答三件事：

| 問題 | 結論 |
|---|---|
| 轉換流程哪一段流失最嚴重？ | **購物車**。481 萬次加購中有 **419 萬次沒有成交（87.0%）** |
| 這個放棄行為是價格造成的嗎？ | **不是。** 五個價格帶的放棄率落在 85.6%–88.1%，全距僅 2.5 個百分點 |
| 能不能在用戶還在站上時預測他會不會買？ | **可以。** 前 20% 高分 session 涵蓋 **43.86%** 的購買者，購買率是平均的 **2.19 倍** |
| 什麼行為最能預測購買？ | **購物車行為**（加購件數、金額、移除次數）佔 **70%** 的預測力；純活躍度幾乎無用 |
| 照這個模型發 10% 折扣做購物車挽回，能多賺多少？ | **在合理假設下仍是負的**（年化 −382,511 ～ −96,849），需要 2.34% 挽回率才打平 |
| 那要怎樣才划算？ | 折扣壓到 **5%**，打平門檻降到 1.02%（僅需訂單成長 5.5%），安全邊際大得多 |

---

## 資料

**來源**：[eCommerce Events History in Cosmetics Shop](https://www.kaggle.com/datasets/mkechinov/ecommerce-events-history-in-cosmetics-shop)
由 [REES46 Marketing Platform](https://rees46.com/) 透過 Open CDP 專案提供 — **真實電商資料，非合成**。

| | |
|---|---|
| 期間 | 2019/10 – 2020/02（5 個月） |
| 規模 | 約 2000 萬筆事件 |
| 事件類型 | `view` → `cart` → `remove_from_cart` → `purchase` |
| 粒度 | 一列 = 一位用戶對一個商品的一次動作 |

**為什麼選這份資料**：多數公開電商資料只有訂單記錄。這份有完整行為流，而且包含 `remove_from_cart` —— 一個罕見且明確的「放棄購買」訊號，正是分析流失的關鍵。

---

## 方法

### 1. 資料清洗與品質檢核
指定型別載入（`category` / `int32` / `float32`）並轉存 parquet，記憶體用量與讀取時間大幅下降。清洗只做三件事，每一項都有明確理由：移除完全重複列、移除 `price <= 0` 的錯誤記錄、移除無 session ID 的事件。

完整品質報告：`data/interim/_quality_summary.csv`

### 2. 轉換漏斗診斷

**分析粒度**：以 `(session, 商品)` 配對為單位，共 **1,380 萬**組。不用事件數計算 —— 同一件商品在一個 session 內可能被瀏覽 5 次，用事件數會嚴重低估轉換率。

| 漏斗階段 | 配對數 | 轉換率 |
|---|---|---|
| 有瀏覽 | 8,309,270 | — |
| 有加購 | 4,811,537 | 瀏覽→加購 **14.98%** |
| 有購買 | 1,278,163 | 加購→購買 **12.99%** |
| **加購後放棄** | **4,186,714** | **87.01%** |

#### 主要發現：放棄購物車與價格幾乎無關

| 價格帶 | 瀏覽→加購% | 加購後放棄% |
|---|---|---|
| 最低價 20% | 19.69% | 85.61% |
| 偏低 | 18.12% | 87.63% |
| 中價位 | 16.31% | **88.14%** ← 最高 |
| 偏高 | 14.71% | 87.54% |
| 最高價 20% | 9.50% | 86.00% |

兩個方向完全不同的訊號：

- **價格強烈影響「要不要加購」** —— 瀏覽→加購率從 19.69% 單調下降到 9.50%，掉了一半
- **價格幾乎不影響「加購後會不會結帳」** —— 放棄率全距僅 2.5 個百分點，且呈微弱倒 U 型，中價位最容易被放棄

**營運意涵**：把折扣預算投在「購物車挽回」的效益有限，因為放棄行為不是價格驅動的。真正的槓桿在商品頁的「加購」這一步。中價位商品是最該優先處理的區段。

#### 資料限制（重要）

**74.14% 的加購配對在同一 session 內沒有瀏覽記錄。** 這表示 `view` 事件的記錄並不完整，因此「瀏覽→加購」這段的絕對數值不可直接解讀為真實轉換率。

不過這個缺失並非隨機：無瀏覽直接加購的比例隨價格單調下降（最低價 81.79% → 最高價 61.01%），符合「低價商品熟客直接補貨、高價商品會先看再決定」的消費行為。

**因此本專案的核心指標鎖定在「加購→購買」這一段** —— `cart` 與 `purchase` 事件都記錄完整，結論才站得住。

其他限制：`brand` 覆蓋率 58.09%、`category_code` 覆蓋率僅 1.81%（品類分析因此不列入主要結論）。

### 3. Session 購買意圖預測
以 session 的**前 10 分鐘**行為預測最終是否購買。

#### 預測問題的定義

> **「一個 session 已經瀏覽了 3 分鐘、目前還沒下單，他最終會不會買？」**

3 分鐘內已購買的 session 直接排除 —— 已經轉換了，沒有東西可預測，留著就是洩漏。這個定義同時也是可上線的介入時機：人還在站上、還沒買、券發得出去。

#### 觀察窗為什麼是 3 分鐘

不是慣例值，是掃描實際分布後的取捨（[完整表格](outputs/tables/observation_window_tradeoff.csv)）：

| 觀察窗 | 符合資格 session | 正樣本% | **保留的購買者%** |
|---|---|---|---|
| 1 分鐘 | 1,249,515 | 11.08% | 88.39% |
| **3 分鐘** | **943,876** | **13.47%** | **81.21%** |
| 5 分鐘 | 786,675 | 14.52% | 72.96% |
| 10 分鐘 | 570,990 | 15.75% | **57.45%** |

窗開得越久訊號越多，但會排除掉越多「早期就下單」的購買者。10 分鐘會讓模型對 **42.55%** 的購買視而不見；3 分鐘保留八成以上，同時仍有 94 萬筆可訓練樣本。

補充：**63.76% 的 session 只有 1 個事件**（一進站就走），這些自動被排除。模型服務的是 20.8% 的流量，但涵蓋 81% 的購買。

#### 三道防洩漏機制（寫成程式碼，不是寫成註解）

1. 只取 session 開始後 3 分鐘內的事件
2. 3 分鐘內已購買的 session 整個排除
3. `purchase` 事件永遠不進特徵，只用來產生標籤

**時間切分而非隨機切分**：2019/10–12 訓練、2020/01 驗證、2020/02 測試。隨機切分等於用未來預測過去。

#### 結果

| 資料集 | baseline | PR-AUC | ROC-AUC | 相對提升 |
|---|---|---|---|---|
| 驗證（2020/01） | 13.16% | 0.2866 | 0.7207 | 2.18x |
| 測試（2020/02） | 12.62% | 0.2853 | 0.7217 | **2.26x** |

驗證與測試分數幾乎一致（0.2866 vs 0.2853）—— 無過擬合、無分布漂移。早停於第 186 棵樹。

**ROC-AUC 0.72 不高，但這是真實的數字。** 用 3 分鐘行為預測購買本來就困難；若這個數字是 0.95，該做的是回頭找洩漏而不是慶祝。程式內建自動警報：ROC-AUC > 0.95 直接標記為極可能洩漏。

#### 十分位提升表（營運端唯一看得懂的績效表）

| 十分位 | 該組購買率 | 提升倍數 | 累積涵蓋購買者 |
|---|---|---|---|
| 1 | 33.05% | 2.62x | 26.19% |
| 2 | 22.30% | 1.77x | 43.86% |
| 3 | 18.00% | 1.43x | 58.12% |
| 5 | 11.51% | 0.91x | 79.18% |
| 10 | 4.11% | 0.33x | 100.00% |

最高分組的購買率是最低分組的 **8 倍**。對「一萬張券該發給誰」這個問題，排序能力比絕對分數重要。

#### 特徵重要度：購物車行為主導

| 特徵 | gain 佔比 |
|---|---|
| `n_cart` 加購件數 | **34.9%** |
| `cart_price_max` 加購最貴品價格 | 12.8% |
| `n_remove` 移除件數 | **11.8%** |
| `events_per_product` 反覆檢視同一商品 | 6.8% |
| `cart_value` 購物車金額 | 6.7% |
| `net_cart` 淨加購件數 | 3.8% |
| … | |
| `n_events` 總事件數 | 0.4% |
| `is_weekend` 是否週末 | **0.0%** |

**購物車相關的 5 個特徵合計佔 70.0% 的預測力。**

兩個發現：

1. **`n_remove` 排名第 3** —— 「加了又刪」確實帶有預測訊號。這正是本專案選用這份資料的理由（多數公開電商資料沒有 `remove_from_cart`），現在被模型證實。
2. **純活躍度幾乎沒有預測力** —— 逛得多、逛得快、逛幾個品牌都不重要。**把東西放進購物車才是意圖。**

> 註：重要度必須看 `gain` 而非預設的 `split`。`split` 只數分裂次數，會嚴重偏袒價格、時間間隔這類連續型高基數特徵。本專案兩種都輸出以供對照 —— `n_cart` 的 `split` 排名接近墊底，`gain` 卻是第一名，兩者結論完全相反。

### 4. 客群分群
【待填】

### 5. 商業影響量化

#### 營收基準（全部由實際資料計算，無假設）

| 指標 | 值 |
|---|---|
| 總 session 數 | 4,537,136 |
| 有購買的 session | 156,574 |
| **session 購買轉換率** | **3.45%** |
| 總營收（5 個月） | 6,348,268 |
| 平均訂單金額 AOV | 40.54 |
| 訂單金額中位數 | 29.29 |
| 每筆訂單平均件數 | 8.21 |
| 平均商品單價 | 4.94 |

客單價約 40、一單 8 件小額商品 —— 典型的補貨型美妝電商。這與模組 1 發現的「低價商品 81.79% 不看就直接加購」互相印證。

#### 情境：對前 20% 高意圖 session 發 10% 折扣

鎖定比例直接讀取模組 2 訓練產出的實際 lift curve（`outputs/models/metrics.json`），非假設值 —— 模型重訓後商業結論會自動更新。

| 挽回率 | 增額訂單 | 佔現有訂單 | 年化淨效益 | 合理性 |
|---|---|---|---|---|
| 1% | 8,388 | 5.4% | **−382,511** | 合理 |
| 2% | 16,775 | 10.7% | **−96,849** | 合理 |
| 3% | 25,163 | 16.1% | +188,813 | 偏樂觀 |
| 5% | 41,938 | 26.8% | +760,138 | 偏樂觀 |
| 8% | 67,101 | 42.9% | +1,617,126 | 不可信 |
| 10% | 83,876 | 53.6% | +2,188,451 | 不可信 |

**「合理性」是自動判定的**：把增額訂單換算成佔現有訂單的比例，超過 30% 一律標記為不可信 —— 沒有任何促銷方案能讓訂單量成長一半。若不設這道檢核，敏感度分析很容易產出幻想數字。

**結論：可信區間內的年化淨效益為 −382,511 ～ −96,849，仍是負的，但已接近損益兩平。**

損益兩平所需挽回率為 **2.34%**（對應訂單成長 12.5%，落在合理範圍）。也就是說這個方案不是必然虧損，而是**成敗完全取決於一個無法從歷史資料得知的參數** —— 只能靠 A/B test 實測。

#### 為什麼會虧：模型越準，虧越多

被鎖定的前 20% session，購買率為 **7.57%**，是全站平均 3.45% 的 **2.19 倍**（與模組 2 實測的 lift 完全吻合，兩個模組互為驗證）。這些人**本來就會買**，折扣對他們是純粹的毛利流失。

這暴露一個根本性的錯配：

> 購買意圖模型預測的是「**誰會買**」（propensity），但發折扣需要知道的是「**誰因為折扣才會買**」（uplift）。模型越擅長找出會買的人，折扣浪費就越嚴重。

真正要解決這個問題需要 uplift 模型，而 uplift 模型需要實驗組／對照組資料 —— 本資料集為觀察性資料，不具備此條件。這是本專案無法克服的限制，在此明確標註。

#### 那要怎樣才划算：折扣幅度是唯一可控的槓桿

| 折扣幅度 | 兩平所需挽回率 | 對應訂單成長 | 可行性 |
|---|---|---|---|
| 3% | 0.58% | 3.1% | ✅ 可行 |
| **5%** | **1.02%** | **5.5%** | ✅ 可行 |
| 8% | 1.77% | 9.5% | ✅ 可行 |
| 10% | 2.34% | 12.5% | ✅ 可行（邊緣） |
| 15% | 4.09% | 21.9% | ⚠️ 困難 |
| 20% | 6.55% | 35.1% | ❌ 不可行 |

**建議折扣 5%。** 打平只需 1.02% 挽回率、訂單成長 5.5%，安全邊際遠大於 10% 折扣方案 —— 後者需要 2.34% 挽回率才打平，而在 1–2% 的合理區間內仍是虧損的。

#### 三個模組的結論互相印證

| 模組 | 發現 | 方向 |
|---|---|---|
| 模組 1 | 放棄購物車與價格幾乎無關（全距 2.5pp） | 折扣不該是主要工具 |
| 模組 5 | 折扣方案在可信區間全數虧損 | 數字證實模組 1 的判斷 |
| 共同推論 | 瀏覽→加購率隨價格從 19.69% 腰斬到 9.50% | 資源應移往商品頁，而非購物車挽回 |

---

## 商業換算的假設（公開透明）

全部定義在 [`src/config.py`](src/config.py) 與 [`src/impact.py`](src/impact.py)：

| 參數 | 值 | 性質 |
|---|---|---|
| 折扣幅度 | 10%（並掃描 3%–20%） | 可控決策變數 |
| 毛利率 | 45% | 美妝電商典型值，**假設** |
| 模型鎖定比例 | 前 20% session | 決策變數 |
| 模型涵蓋率 | **43.86%** 的購買者 | **模組 2 實測值**，非假設 |
| 折扣挽回率 | **刻意不設定單一值** | 無法從歷史資料推得，改以敏感度分析與損益兩平點處理 |

⚠️ **這是情境試算不是實測結果。** 挽回率只能靠 A/B test 實測 —— 這也是為什麼本專案的核心產出是**損益兩平門檻**而非單一預估值：門檻不依賴任何樂觀假設。

---

## 如何執行

```bash
pip install -r requirements.txt
```

設定 Kaggle API 金鑰（[取得方式](https://www.kaggle.com/settings)），然後：

```bash
python -m src.download && python -m src.prepare
```

---

## 專案結構

```
├── src/
│   ├── config.py       # 路徑、型別、模型參數、商業假設
│   ├── download.py     # Kaggle 資料下載
│   └── prepare.py      # 清洗、轉 parquet、品質報告
├── docs/
│   └── 專題規格書.md    # 完整規格、實作順序、防雷清單
├── data/               # 原始與中繼資料（不進版控）
└── outputs/            # 圖表、表格、模型（不進版控）
```

---

## 資料授權

資料集由 REES46 Marketing Platform 提供，可免費使用並須標註來源。
本專案程式碼僅供學習與作品集展示用途。
